export interface IEmptyListData {
  header?: {
    text?: string;
    actionButton?: { glyph: string; text: string };
  };
  body?: {
    text?: string;
    actionButton?: { glyph: string; text: string };
  };
}
