import React from 'react';
import { Tab, ReactMarkdown } from '@kyma-project/react-components';

export const DocumentationTabs = () => {
  // if (!docs) {
  //   return null;
  // }
  // console.log(docs);

  // return docs.map(type => (
  //   <Tab title={type.metadata.title} key={type.metadata.title}>
  //     <ReactMarkdown source={type.source} />
  //     {type.source}
  //   </Tab>
  // ));
  return <Tab title="asd">asdasda</Tab>;
};
