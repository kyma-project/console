{
  "name": "ui-tests",
  "description": "Smoke tests for Console",
  "scripts": {
    "test": "PASSWORD=$(kubectl get secret admin-user -n kyma-system -o jsonpath='{.data.password}' | base64 -D) testcafe chromium tests/tests.js",
    "test:headless": "PASSWORD=$(kubectl get secret admin-user -n kyma-system -o jsonpath='{.data.password}' | base64 -D) testcafe chromium:headless tests/tests.js",
    "test:cluster": "testcafe chromium:headless tests/tests.js"
  },
  "devDependencies": {
    "testcafe": "^1.8.3"
  }
}
